apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability
# Compiled PrometheusRule manifests are produced by `sloth generate` in CI and
# committed alongside the SLO definitions. The CI workflow lives at
# .github/workflows/sloth.yml.
resources: []
